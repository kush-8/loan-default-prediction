# Lean requirements for the prediction API Docker image.
# Pinned to versions matching requirements.txt for reproducibility.
# Training-only dependencies (optuna, kaggle, matplotlib, shap, etc.)
# are intentionally excluded to keep the image small.
fastapi==0.116.1
uvicorn==0.35.0
scikit-learn==1.7.1
pandas==2.3.1
lightgbm==4.6.0
pyyaml==6.0.2
pydantic==2.11.7
joblib==1.5.1
httpx==0.28.1
numpy==2.2.6